export enum TagType {
  SKILL = 'skill',
  BENEFIT = 'benefit',
  LANGUAGE = 'language',
  FRAMEWORK = 'framework',
  CONTRACT = 'contract',
  LOCATION = 'location',
}

export const TAG_TYPE_VALUES = Object.values(TagType) as string[]
